class: Student {
  initialize grades to []
  initialize avg_grade
  initialize passing

  func: Student() {
    set avg_grade to 100
    set passing to true
  }

  func: add_grade( grade ) {
    add grade to grades
  }
}